DESCRIPTION="GNOME font management application"
HOMEPAGE="http://code.google.com/p/font-manager/"
SRC_URI="http://font-manager.googlecode.com/files/${P}.tar.bz2"

DISTCLEANFILES="src/ui/export.py"

src_compile() {
	cd ${S}
	cygautoreconf
	lndirs
	cd ${B}
	cygconf --enable-winlib
	cygmake
}
